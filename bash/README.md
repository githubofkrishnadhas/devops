This folder is used to run a simple Jenkins Job. 

In the Jenkinsfile it is instructed to a run on a docker agent with image zenika/terraform-azure-cli:latest 

on top of this the job will execute the comments mentioned in jenkinsfile as well as the scripts.

In this scenario only echo is executed .